#!D:/utilities/nou
#!/usr/bin/nou
~[bind ~]
~[]
bind let, ~;
bind block, %;
bind lambda, \;
bind 
bind define, \[[symbol term] bind [symbol term]];
bind 

define defmacro [list \[[body] [
  
]]]


define each [x y] [
  
]


define write-file [filename mode data] [

]

$[filename]
open $[filename] => #[filename stream] -> ~[stream]
open [filename mode] => #[filename stream]

